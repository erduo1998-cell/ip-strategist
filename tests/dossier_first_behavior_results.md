# 档案优先运行门行为测试记录

- 日期：2026-08-13
- 宿主：Claude Code 2.1.220，三次互不共享上下文的非交互、无会话持久化运行。
- 输入边界：仅当前仓库 Skill 路径、用户原话和场景必需的虚构工作目录；未提供本文评分标准、其它会话输出或本次修复说明。
- 隐私：所有状态均为仓库内虚构 fixture；未使用或读取真实用户档案。
- 透明限制：Codex CLI 的两次独立尝试均在模型采样层超时，已终止且不计作通过或失败；下表只记录实际返回完整答案的 Claude Code 运行。

| 用例 | 结果 | 可复核的用户可见行为 |
|---|---|---|
| 1. 首次真实任务不能绕过建档 | 通过 | 明确“暂不直接给稿”；说明私人路径与用途，请求一次同意；把原题保留为建档证据和待办 |
| 2. 建档中途不能被新任务带走 | 通过 | 用档案目标指出“涨粉”可能不是首要问题；不交付增长方案，只从 `audience` 断点问一个目标用户问题 |
| 3. 已有档案必须纠正表层问题 | 通过 | 明确“你诊断错了”；引用 90 天目标、明确不追项和两条虚构数据，拒绝加强 CTA，只要求核对主页访问与私信问题类型并继续流程诊断内容 |

结果记录保存最小可复核事实，不冒充保存了完整模型日志。确定性路由、摘要字段过滤和双 Python 环境回归由仓库 unittest 另行覆盖。
