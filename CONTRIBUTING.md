# 贡献指引

欢迎给 ip-strategist 提 issue 和 PR。在动手之前，请先花两分钟读完这一篇——它能帮你判断「这个改动该不该提、提了合不合适」，省得白干活。

## 方法论内核：稳定优先

`references/00-11` 十二个维度文件里的**判断心法本身**（五条底层心法、诊 / 契 / 行 / 盘四相、需求倒推、骨架六种推进动力、增长单元、初始化建档诊断、控制变量与阶段识别……）是这套 skill 的根，经过一线实战反复打磨，追求**长期稳定**，不轻易改。

所以——

- **不欢迎**重写方法论内核、替换判断框架、把某条心法推倒重来的 PR。这类改动几乎不会被合并，别在上面花时间。如果你确实认为某条心法有根本性错误，请先开 issue 说明你的论据和反例，由维护者判断。
- **欢迎**修文档一致性、补示例、做平台适配、补边界声明、修断链、补伦理护栏、改进安装体验——这些围绕内核的"外圈"工作，是开源协作的主战场。

一句话：**修"怎么把这个方法论传达清楚 / 用得更顺"，不要改"方法论本身"。**

## 欢迎的贡献类型

- **文档与一致性**：修错别字、修断链、统一术语（如四相 vs 四阶段）、补术语白话解释。
- **平台适配**：补充非抖音平台的播放量口径、节奏差异（视频号 / 小红书 / B 站 / 公众号等）。
- **示例补充**：新的脱敏案例、新的示例对话片段、新场景的使用示范。**严禁**在示例里塞入任何真实个人 / 课程 / 社群 / 平台账号信息——本 skill 全程脱敏，示例也不例外。
- **边界澄清**：划清本 skill 与剪辑 / 后期 / 动效工具的分工，避免用户误以为无所不能。
- **伦理护栏**：在"给希望 / 给情绪出口 / 给理由"类指导处补事实红线（观点可以给力量，事实不可造假）。
- **安装与排障**：补 TROUBLESHOOTING、补新 agent 的 skills 目录路径、改进首次使用体验。
- **references frontmatter 修正**：发现 `upstream` 指向不存在的文件，欢迎修。

## 提 issue

- **bug / 文档错误**：说清在哪个文件、哪一段、错在哪、你期望的正确表述是什么。
- **平台适配建议**：说清是哪个平台、差异点、建议怎么补。
- **新示例 / 新场景**：先说清这个示例服务于哪类用户、对应哪个维度（00-11）。
- **方法论质疑**：说清你不同意哪条、给出具体反例或论据。空泛的"我觉得不对"没法跟进。

## 提 PR

1. **先开 issue 对齐**（尤其涉及多文件或新增文件的改动），避免做完才发现方向不对。
2. fork → 分支 → 改 → 提 PR。分支名写清意图，如 `docs/fix-dimension-count` / `platform/add-xhs-note`。
3. PR 描述写清：改了哪些文件、为什么改、对应哪个 issue。
4. **只改与本次意图直接相关的行**。不要顺手"美化"无关段落、不要重排不相关格式——diff 越聚焦越好审。
5. 涉及 `references/00-11` 内容的改动，**必须**在 PR 描述里说明：这一改是修一致性 / 补护栏 / 补边界（外圈），还是动了心法本身（不会被合并）。

## 来源与隐私红线

本 skill 的核心方法论来自耳总个人长期实践经验。若某个新增框架参考了第三方公开观点，必须像 `references/10-增长与系列.md` 一样保留来源与吸收边界，不得去除来源后重新署名。仓库中的案例和贡献材料必须去除可识别的私人、客户与商业信息；公开来源归因不属于示例泄露，人设锚点仍为虚构公开示例，用户需自建，不套用任何真人信息。贡献内容：

- 不得出现任何真实个人姓名、课程名称、社群名称、平台账号信息。
- 示例里的人名一律用化名，公司一律用虚构名，案例一律用脱敏改写。
- 不得在公开文件（CHANGELOG / README / 文档 / 示例）里引用任何被 `.gitignore` 排除的私有锚点文件。定位锚点统一用脱敏公开的 `references/定位锚点-示例.md` 作为格式参考；用户照字段往自己的 `ip-dossier.md` 底座区填，不套用任何真实人物信息。

## 不在范围内

以下不属于本 skill 的职责，相关 PR 不会被合并：

- 视频剪辑、后期制作、字幕轨、节奏拼接（交给 `jianying-editor` / `video-use` / 剪映 / CapCut / Premiere / 达芬奇）。
- 视觉动效、B-roll、转场特效、信息图动画（交给 `hyperframes` / `remotion`）。
- 把 04 的「语气指南」删掉或外挂化。语气指南是 skill 自带的，保持自包含。

## A 轨结论如何进入 references/01、02

`SKILL.md` 里的 A 轨（内容认知）会在陪跑过程中持续产生判断。这些判断首先沉淀在**用户私有的陪跑档案**（`ip-dossier.md` 及契约文件）中，**agent 不会自动把它们写进 `references/01` 或 `references/02`**。

只有当某个判断被验证为「跨个体通用」时，才会通过 curated 回流进入公共 references。流程如下：

### 1. 什么算「跨个体通用」

必须同时满足：

- 在 **≥3 个不同创作者**身上被验证过；
- 与具体人设、行业、平台无关，是可迁移的内容打法判断；
- 有明确的适用边界和可观察的反例，不是拍脑袋的绝对真理。

例如「选题要用需求 + 方案双向奔赴」是通用判断；「律师号一定要拍法条解读」不是。

### 2. 如何提交

开 issue 或提 PR，**标题前缀 `[A轨回流]`**。

正文必须包含：

- **判断原文**：你要回流的那个判断，一句话说清楚；
- **验证次数**：在几个创作者身上验证过，分别处于什么阶段/行业（可匿名）；
- **来源契约编号**：对应私有契约文件的编号或会话标记（可匿名，如「C-2025-xxx」）；
- **适用边界**：这条判断在什么条件下成立；
- **反例说明**：在什么情况下它**不**成立，或会失效。

示例：

```markdown
[A轨回流] 选题判断：钩子前置时，前 3 秒必须出现具体代价而非抽象结论

- 判断原文：口播视频前 3 秒如果给的是抽象结论（如"很多人做错了"），完播率普遍低于给出具体代价（如"这一步让你少赚 3000"）。
- 验证次数：4 位创作者（企业咨询、求职辅导、健身、母婴），均出现同向偏差。
- 来源契约编号：C-2025-011、C-2025-018、C-2025-022、C-2025-029（已脱敏）。
- 适用边界：口播类短视频（30-90 秒），目标动作为「看完/转化」，非剧情/非纯展示类。
- 反例说明：剧情号、vlog、强视觉钩子类内容不适用；账号已建立强信任后，抽象结论反而更有效。
```

### 3. 维护者 review 标准

维护者会从四个维度审：

- **可复现**：判断背后的观察是否可被他人复现，而不是单次灵感；
- **足够通用**：是否真正跨人设、跨行业，还是只是某个账号的特例；
- **有反例**：是否主动声明了边界和失效场景；
- **值得进公共方法论**：是否对 `references/01`（定位与人设）或 `references/02`（选题方法论）有结构性补强，而不是边缘补充。

### 4. 发版合并流程

- **不自动写入**：即使 review 通过，也不会立即合并到 `references/01` 或 `references/02`；
- **发版窗口期 curated 合并**：维护者在发版窗口期人工挑选、改写、归位，确保语气和结构与原文件一致；
- **合并后同步更新**：修改 `CHANGELOG.md` 说明新增/修订了哪条判断，并在对应 references 文件的版本标记或 frontmatter 中更新版本号；
- **用户私有档案不受影响**：回流只改公共 references，不改任何用户的私有 `ip-dossier.md` 或契约文件。

### 5. 明确边界

- **agent 只写用户私有档案**：陪跑中产生的 A 轨判断，agent 只写入当前工作目录的 `ip-dossier.md` 和契约文件；
- **references/01、02 只能由维护者写**：公共方法论文件的 curated 合并是维护者的人工职责，不是 agent 的自动行为。

## License

贡献一旦合并，即按 [MIT](./LICENSE) 许可发布。

## 行为准则

保持务实、对事不对人。提问前先翻 [TROUBLESHOOTING](./TROUBLESHOOTING.md) 和 [CHANGELOG](./CHANGELOG.md)，避免重复问已知问题。
