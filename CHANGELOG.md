# 更新日志

本文件记录 ip-strategist skill 的版本演进。版本号标在 SKILL.md 正文（metadata 字段视 agent 加载兼容性决定是否写入 frontmatter，不强制）。

方法论文件（`references/00-11` 十二个维度里的判断与机制）追求长期稳定，不随版本随意改动；大部分版本变更集中在**文档一致性、安装门槛、边界清晰度、伦理护栏**这些围绕内核的外圈。

## [1.9.0] - 2026-07-20
### 初始化建档诊断

- 新增 `references/11-建档诊断.md`：目标、证据、用户、价值、商业与行为、执行与红线六模块自适应访谈，每轮只问一个核心问题。
- 模式 A 新用户不再复制空模板后放行；完成六模块后先展示完整 v0.1 草案，用户一次明确确认后才写入 `provisional` 档案。
- dossier schema 升至 1.9，新增 `onboarding_status: in_progress | provisional | confirmed` 与 `onboarding_step: goal | evidence | audience | value | business | execution`，支持访谈中断续聊。
- 档案模板增加 90 天主目标、成功标准、用户具体状态、信任依据、当前/未来价值、主行为、执行资源、红线、最大未知与依据账本。
- `ip-check.py` 新增建档状态、断点、核心字段和依据账本检查；`in_progress` 只提醒续访，`provisional / confirmed` 缺核心字段则报错，不再把「有文件」误判为「已完成建档」。
- 已有完整档案跳过完整访谈，只缺核心字段时针对性补问；主动换定位只重跑受影响模块并保留底座修订留痕。
- 模式 B 保持轻量；目标明显不清却直接要内容时，最多做「给谁看 / 希望看完做什么 / 凭什么由你讲」三问微诊断；仍不清时只产出标明假设的探索稿。
- 新增结构测试、`ip-check` 状态测试和 10 个互不泄漏标准答案的干净会话行为前测。诊契行盘、四阶段、契约 7 个机器字段、隐私纪律和 references 只读规则保持不变。

## [1.8.0] - 2026-07-20
### 增长系统层

- 新增 `references/10-增长与系列.md`：增长单元四问、主验证目标、关注三路径、系列六零件、账号记忆资产、起号发布战役、搜索长尾与平台事实门。
- 把增长诊断改为“主干 + 分支”，不再把搜索、收藏、关注和转化硬串成单一直线。
- 纠正“关注只是最高情绪阈值”“归属感是唯一深层原因”等绝对表述，并把平台绝对播放门槛降级为待核验经验。
- 契约、档案和周计划增加增长字段；档案 schema 升至 1.8，契约 7 个机器字段与四相/四阶段保持不变。
- 增加增长结构测试和干净提示行为前测；更新 README、示例与项目能力路由。
- 增长层结合公开行业讨论中关于关注与系列的可迁移问题，经维护者重构为可验证框架；不吸收第三方人设、案例、收益宣称和未核验平台数字。

## [1.7.0] - 2026-07-19
### 发布前可信度修复

- 明确方法论来自耳总长期 IP 实战、影视导演、AI 咨询和创作者陪跑的个人经验沉淀，不是第三方课程或作品的改写、摘编或重新署名。
- 契约新增 `review_after_days`；待发布时 `next_review_date` 留空，实际发布后按 `actual_publish_date + review_after_days` 生成。
- `ip-check.py` 新增日期合法性、日期顺序、终态残留和空索引检查，并支持 `--sync-index` 原子重建索引。
- 契约确认收敛为一次明确确认；完整展示草案后，用户无歧义同意即可写入。
- 统一隐私与能力边界：状态文件默认不进公共仓库；可给投放、团队和直播策略，但不代执行运营动作。

## [1.6.0] - 2026-07-16
### 状态机升级：已废弃状态 + 模式 A/B 持久化 + 契相确认收紧 + 实际发布日重算

本版本围绕"让状态机覆盖选题取消场景、让模式选择跨会话延续、让契约签署更不容易误签、让复盘提醒日期更准"四个点加固陪跑机制。

- **新增「已废弃」状态**：契约生命周期状态从三档扩展为四档——「待发布 / 待复盘 / 已复盘 / 已废弃（已取消）」。「已废弃」用于选题决定不发、方向被推翻等场景；已废弃契约不纳入「最早复盘提醒」和「待发布超期」计算，但保留在 `ip-contracts/` 作为决策留痕。`templates/dossier-template.md` 的契约索引区、`templates/contract-template.md` 的 frontmatter 注释、`references/08-陪跑闭环与档案.md` 的状态说明同步更新。
- **模式 A/B 持久化**：档案顶部「当前状态」区新增「默认模式：A（陪跑）/ B（单次咨询）」字段。档案存在且设定了默认模式时，启动协议优先按档案默认模式处理；用户本次仍可声明「按 B 处理但不改默认」，仅当次轻量处理，不覆盖档案中的默认模式。单次咨询用户改主意可随时升级建档；陪跑用户某次只问独立问题也可切 B。
- **历史确认机制**：该版曾采用多轮确认；v1.7.0 已收敛为完整展示草案后的一次明确确认。
- **实际发布日回填后重算下次复盘日**：回填 `actual_publish_date` 时，若 `next_review_date` 此前是按 `plan_publish_date` 估算的，必须按 `actual_publish_date + T+N` 重算并更新契约头部的「下次复盘日」字段。
- **版本号全仓库对齐 1.6.0**：README / SKILL.md frontmatter / 示例对话 / dossier `schema_version` 统一拉到 1.6.0；contract-template 增加 frontmatter 唯一可信源警示。

## [1.5.0] - 2026-07-16
### 系统一致性大修：A 轨回流重定义 + 字段 YAML 化 + 契约闸门硬化（第三方审查 + 自审驱动）

基于第三方审查与一轮自审，做一轮围绕"把自动进化机制做安全、把状态字段做健壮、把语气标签做干净"的系统修复。方法论内核（四相 / 契约 / 双轨进化 / 五心法）不动，全部集中在外圈工程一致性与落地。

- **A 轨回流重定义（审查最致命项）**：`references/01-定位与人设.md` + `references/02-选题方法论.md` 定性为【维护者维护的只读通用方法论弹药库，agent 只读、永不写】。A 轨（内容认知）被验证的结论【写进本档案 `ip-dossier.md` 的认知沉淀区 A 轨】；跨个体真正通用、值得进 references 的判断，由【维护者在发版时人工 curated 合并进 references】（流程进 CONTRIBUTING.md，本次该文件若不存在则暂以一句话说明，下版补流程），agent 绝不自动写公共 `references/`。治"agent 把判断自动写回 references"造成的污染 + 只读写入失败两层风险——全仓库凡是"回流双文档 = agent 写回 references/01、02"的措辞，统一改成这层意思。
- **契约 / 档案 definitive 字段 YAML frontmatter 化**：`contract-template.md` 顶部加 YAML frontmatter（contract_id / status / sign_date / plan_publish_date / actual_publish_date / next_review_date 六字段）；`dossier-template.md` 的 `schema_version` 改 YAML frontmatter（值 1.5），与人读说明并存。治 LLM 解析自由文本头部出错的脆弱性。
- **契约确认闸门**：该版首次要求先展示草案、再取得用户明确同意，禁止 agent 自问自答代签；v1.7.0 进一步去掉重复确认。
- **契约状态机去 hack**：已复盘契约的 `next_review_date` 字段清空（不再塞"已结案"字符串）；"最早复盘提醒 = min(next_review_date)"计算时跳过 status=已复盘 的契约。状态三档（待发布 / 待复盘 / 已复盘）不变。
- **发布回填与逾期机制**：实际发布日期由创作者告知 / agent 主动询问后回填，状态随之待发布 → 待复盘；待发布契约若超过预计发布日 N 天仍未回填，提醒时标注"待发布（可能已发未回填）"，不混入复盘催办。
- **<3 条复盘口径三处统一**：SKILL / `references/08-陪跑闭环与档案.md` / `docs/示例对话.md` 统一为"不拒盘，降级定性分析（标注置信度低、结论停'待验证'、不写进认知沉淀区）"；删除"挡回去" / "单条不盘" / "至少三条才盘"这类硬挡措辞。
- **阈值统一**：「同一判断被（不同内容 / 不同时间）验证 ≥3 次」= 写进认知沉淀区下结论的统一门槛；「3-5 条同向样本」明确标注为"样本条数锚点（可下轻结论，仍不重押）"，与"验证次数"区分清楚，避免数字相近混淆。
- **开机 vs 契约桥接**：心法 5「开机比研究快」旁加一句——"开机快 ≠ 不签契约：签的是判断、不是犹豫；开机省的是过度研究，不是省掉想清楚这条赌什么"。
- **模式 A/B 语气传染修复**：README 与 `docs/示例对话.md` 里凡"模式 A/B = 语气"的用法，全部改为「对内语气（对创作者说话）/ 对外语气（写对外内容）」；"模式 A/B"标签只在"建不建档（走不走陪跑档案 + 契约）"语境用。延续 [1.3.2] SKILL 内术语解耦，把残留在 README / 示例对话里的旧用法一并扫清。
- **README 诚实化与边界声明**：补「不适合谁」+ 与通用 AI / 真人教练对比；全链路措辞软化为「脑力活全链路」；付费入口加诚实声明——"高 stakes 判断（定位诊断 / 瓶颈突破 / 复盘归因）本 skill 给框架和方向，但最终人脑把关更稳；作者耳总提供可选付费 1v1"，把"卡住 → 引导付费"重新框定为"承认 skill 边界 + 可选升级"，而非推销。
- **references 措辞与护栏**：`03`「信念感」改为可观测代理信号；关于"观点与事实"的护栏前置强化（观点可给希望，事实不可造假）；`06`「投放羞耻症」措辞软化 + 阶段边界强化；`02` 补「选题自检三问」。
- **新增模板与脚本**：`templates/weekly-plan-template.md`（周计划模板）、`scripts/ip-check.py`（状态一致性校验）。
- **版本号全仓库对齐 1.5.0**：README / 示例对话此前分别卡在 1.3.0 / 1.2.0，本次统一拉到 1.5.0；dossier `schema_version` = 1.5；示例对话声明对齐 SKILL v1.5.0。

**方法论沉淀（高价值）**：开源 skill 的"自动进化回流"若指向安装目录的公共文件（如 `references/01、02`），会同时制造两层风险——一是污染（不同用户的私有结论互相覆盖、混进公共方法论），二是只读写入失败（安装目录常无写权限 / 升级时被覆盖）。安全的做法是 agent 只写用户私有副本（本档案认知沉淀区），真正通用的结论由维护者在发版时人工 curated 合并进 references。"自动进化"应停在私有副本层，跨个体的公共方法论进化必须经人脑闸门。

## [1.4.0] - 2026-07-16
### 进化方向补齐：认知沉淀区分双轨，让"对这个人的理解"也能进化

原有进化闭环（契约 → 盘 → 认知沉淀区 → 回流双文档）跑得很完整，但验证的全是**内容判断**（钩子 / 骨架 / 选题管不管用），进化的产物回流进跨创作者的通用方法论（双文档）。而 skill 核心价值之一"对这个创作者越来越懂"——这层**对人判断**没有赌注-验证闭环：诊相的"卡点判断"说完就过，底座区被当静态锚。结果是系统在进化"通用方法论"，没在进化"对这个人的理解"。

本次把内容判断已跑通的"赌注 → 验证 → 记证伪 gap"四件套，**镜像一份给对人判断**（不新建系统，复用骨架）：

- **认知沉淀区分裂双轨**（dossier §四 + SKILL 术语 + 08 §三④）：A 内容认知（内容打法判断，可回流双文档）/ B 对这个人认知（对人的判断、尤其被证伪的，只留本档案不外流）。B 轨缺位 = 内容方法论在涨、对这个人的理解没涨。
- **诊相加「对人赌注」**（SKILL 工作流 §1 + 08 §一）：诊相产出从"一段判断"扩为"判断 + 一条可被打脸的对人赌注"，落进 B 轨待验证。诊相赌的是这个人，跟契相的内容赌注平行，不混。
- **盘相验证对人赌注**（SKILL 工作流 §4）：盘相 ② 在验证契约（内容判断 → A 轨）之外，同时验证诊相留下的对人赌注（→ B 轨，证伪必写原因）。
- **底座区带版本**（dossier §一 + 08 §三①）：底座要素改了要留痕（同契约"发布即冻结 + 修订"），从一次性锚定变随验证缓慢进化。
- **contract-template 归档去向标 A 轨**：契约验证的是内容打法，明确进「认知沉淀区 · A 内容认知」，B 轨另由对人赌注来。
- dossier schema 1.3 → 1.4。

**方法论沉淀（高价值）**：一套进化机制"进化什么"，由它验证什么决定。验证内容 → 进化通用方法论；验证人 → 进化个体理解。两者必须分轨存、分轨回流，否则个体理解会被通用结论淹没。同理适用于任何"把软判断仪表化"的设计——别让容易量化的（内容打法）挤掉难量化但更重要的（对这个人的理解）。

## [1.3.2] - 2026-07-16
### 模式定义 axis 解耦 + 变现核心框架提炼（内部场景检查驱动）

跨会话、选题、定位、变现和阶段五类内部场景检查发现两类问题，本次修复。历史检查未形成可公开复现的评测集，因此不保留模型分数。

**P1 · 模式 A/B 的双 axis 混淆（E/F/G 三场景共性）**
SKILL 原把「建不建档」和「对谁说话（语气）」两个正交维度硬绑进一个「模式A/B」标签，导致单次定位 / 变现 / 阶段诊断时内部标签摇摆。根因是语气段用 A/B 命名语气，与启动协议「模式A/B=建档」字面冲突。

- **术语彻底收敛**：「模式A/B」**只在建档语境**用（走不走陪跑档案+契约）；语气一律改用「对内语气 / 对外语气」。语气段九处正文定义 + L17 人设红线定义全部解耦。
- 模式判定入口（L83）加硬说明：「模式A/B 只决定建不建档，不决定语气；单次诊断=建档B + 对内语气，两者并存」。
- 语气切换段（L193）去 A/B 绑定，明示与建档模式独立。

**P2 · 变现场景漏用「博主三路径」**
`references/06` 的「博主三路径」（围绕粉丝卖东西 / 接广告有上限被动 / 导流赚差价）是 06 的核心判断工具，首轮内部场景检查却漏用，转而套了通用阶梯模板。根因是读取回执只要求提炼「独有方法」，容易停在中段技巧而漏掉核心判断框架。

- 读取回执硬指令（L130）从"提炼独有方法"升级为"提炼**核心判断框架**（能决定建议方向的，非中段技巧）"，并举例：变现的「博主三路径」、选题的「需求×方案双向奔赴」、阶段的「四阶段状态机」。

**重测观察**
- 变现场景开始使用三路径做主动 / 被动对比。
- 阶段与定位场景不再混用建档模式和表达语气标签。

**方法论沉淀（高价值）**：规则在文档里出现**两处不一致的定义**（如语气段用 A/B 命名语气 vs 启动协议 A/B=建档）时，agent 读正文定义优先、声明压不住。**解耦必须改所有出现处，不只加声明**——只加 axis 解耦声明不够，把正文旧术语全部替换后才治本。同 [1.3.1]「D1 硬化」教训一脉相承：治本要落到控制层的每个触发点，不是加一句总声明。

## [1.3.1] - 2026-07-16
### 跨会话状态机制硬化（内部场景检查驱动）

延续 [1.3.0] 的执行一致性优化，把 D1 状态机制从「概念知识」升级为「入口强制动作」。内部检查发现 agent 虽能复述原件优先规则，却可能继续照索引或用户提供的冲突值行动。

- **读状态门加「读档即比对」硬步骤**:模式A 读档时逐份打开 `ip-contracts/` 每份契约原件读头部状态,逐行对照档案索引表;任何一行不一致,必须在回复里显式声明「索引表显示 X、契约原件显示 Y,以契约原件为准」并以原件状态进入对话。把「每次会话自动重建」的被动叙述改为「读档即比对 + 不一致即声明 + 以原件执行」的主动硬流程。
- **复盘相加第③步「复盘收尾」(治状态残留)**:复盘完成时,契约头部「下次复盘日」清空或标「已结案」(不再参与 min 计算),并重算档案 header「最早复盘提醒」=剩余**未结案**契约下次复盘日的 min(无则留空)。治复盘后契约/提醒字段 stale、下次会话拿过期提醒催的残留。
- **新词「已结案」范围限定**:仅作契约头部「下次复盘日」字段在契约已复盘后的标注值(语义=该契约复盘周期已终结),**非状态枚举的扩展**——状态仍三档(待发布 / 待复盘 / 已复盘),「已结案」只描述下次复盘日字段的生命周期终结。
- **补 SKILL.md 版本记录**:补回上版漏更的 v1.3.0 条目(SKILL.md 版本段此前停在 v1.2.0)。
- 内部重测确认 agent 能显式报告索引与原件冲突，并完成复盘收尾；该历史检查未形成可公开复现的评分基准。

## [1.3.0] - 2026-07-16
### 执行一致性优化（内部端到端场景检查驱动）
- 读取门加「读完回执」:动笔前必须输出读了哪些 reference(含 00)、每份提炼 1 个独有方法、本任务怎么用——把"读过"变"消化能用",治读而不化
- 00 提为读取门硬指令(原表格泛化行易漏读)
- 模式B 改「显式假设驱动」:动笔前列默认人设/红线假设、默认推进、邀请纠正(替代模式B 下过死的"问完再动笔";模式A 仍走完整问完再动笔)
- 内部场景检查确认写稿和复盘能调用对应的核心框架；历史检查未形成可公开复现的评分基准。

## v1.2.0 — 一致性大修（2026-07-16）

以 SKILL.md 为 canonical 源，对全库做一次系统性字面对齐。修复一批跨文件互斥表述、死条款、失真版本声明与不诚实声明。本次方法论内核（五条心法与各维度判断）不动，只在外圈做一致性收敛。

### 状态机制修复（D1，高危死循环）
- **契约原件头部 = 状态唯一可信源**：编号 / 状态 / 签订日期 / 预计发布日期 / 实际发布日期 / 下次复盘日六字段只存在契约原件头部。
- 档案内的**契约索引表收敛为只读视图**，每次会话从所有契约原件头部字段聚合重建，自身不可写、不存状态。
- 三方表述统一：SKILL.md / `contract-template` 头部字段 / `dossier-template` 索引表只读声明一致。彻底删除「改索引表状态但不动原件」「索引表可写」「从契约头部重建索引却又要求改索引」的互斥表述。

### 字段补全与复盘提醒（D2，高危2）
- 契约头部 definitive 字段补全为 6 个：编号 / 状态 / 签订日期 / 预计发布日期 / 实际发布日期 / 下次复盘日。
- 「下次复盘日」改为按 T+N 规则换算成绝对日期（YYYY-MM-DD）写入头部，不再是正文里无法定位的相对 T+N。
- 档案顶部「⏰ 最早复盘提醒」= min(所有契约「下次复盘日」)，无契约则留空不催。

### 档案四区统一（D3）
- 全库（08 / `dossier-template` / SKILL.md）统一四区：①底座区 ②契约索引区 ③数据快照区 ④认知沉淀区。SKILL.md 此前漏写「数据快照区」，本次补回。

### 五条心法对齐（D4）
- SKILL.md 的「五条底层心法」与 `references/00-心法与反模式.md` 字面一一对应定稿。合并统一为三条核心判断心法（判断力总纲 / 市场与观众 / 用户思维）+ 两条执行心法（落实日常计划 / 开机比研究快）。

### 进相阈值统一（D5，修倒挂）
- 统一为「同一判断被验证 ≥3 次 → 写入认知沉淀区」，**不是 10 条**。
- 认知沉淀区条目必须有「验证次数」字段，否则不升级。

### 归档三档词汇统一（D6）
- 全库统一三档：**已验证 / 待验证 / 已证伪**。替换一切「命中 / 部分对 / 翻车 / 存疑」等同义异写。

### 双文档定义（D7）
- 归档去向收敛为有实际容器的选项：进入认知沉淀区 / 回流到双文档。
- SKILL.md 明写「双文档」= `references/01-定位与人设.md` + `references/02-选题方法论.md`，不再出现未定义的「双文档」。

### 契相豁免与模式 A/B（D8/D9，修死条款）
- 删除原三个互抵的 AND 死条款（凑齐多个前提才能豁免）。
- 改为：**模式A · 陪跑**（全套契约 + 档案，复盘写回触点收敛到 ≤3 处）与 **模式B · 单次咨询**（即轻量入口，不签契约、不建全套档案，适合一次性提问 / 诊断）。
- SKILL.md 启动协议明列模式 A/B 与判断条件；模式 B 是无条件轻量入口，不必凑齐多个前提。

### 数据 <3 条降级（D10，修落地缺口）
- 用户带 <3 条数据不硬挡。改为降级「定性分析」，标注「数据不足，置信度低，结论待验证」，提示补数据后升级。
- 「3-5 条同向样本」只是样本条数锚点（可下轻结论，仍不重押）；写进认知沉淀区需「同一判断被不同内容/不同时间验证 ≥3 次」；分析本身不拒绝。

### references 强制读取门（D11）
- SKILL.md 用「任务→必读文件」映射表取代泛泛「按需读取」，内联进启动协议：写稿 → 04（+03 骨架 + 05 表现力）；选题 → 02；定位 / 人设 → 01 + `定位锚点-示例.md`；复盘 → 07 + 08；阶段判断 → 09；变现 → 06；心法 / 反模式 → 00。

### 脱敏与锚点修正（D12/D13）
- 清除「耳总」「刘冉」「知识库选题工厂」「只落 agent 执行层」「反问耳总」等私有 / 作者黑话，改中性表述（「向用户澄清」「只产出 agent 可执行层」）。
- 人设示例（发现者不教训者、外行听得懂等）若保留，标注「示例」。
- 公开仓库不再引用 gitignore 排除的 `references/私有定位锚点文件.md`，全部改指 `references/定位锚点-示例.md`（公开脱敏版）。字符串「私有定位锚点文件」只允许出现在 `.gitignore`。

### 并发写承诺务实化（D14）
- 删除「内容明显被别处改过」这类跨会话无基线、做不到的检测承诺。
- 改为：写前重读目标文件最新内容、写后提示「本次修改了 X 处」；原子性靠「单 agent 单文件单次写」纪律保证，不夸大。

### 方法论矛盾调和（D15）
- 06「几乎不花钱」vs「月烧九千」→ 起步期近乎零成本验证假设；规模化放量需投入（月烧九千为规模化案例）。
- 06「一条视频就能起来」vs 07「两条就下结论是错误」→ 单条爆款用于验证核心假设；账号能否持续须多条数据，不可两条定论。
- 07「播放量无参考价值」vs「按播放量分层最实用」→ 绝对数值无参考价值；按量级相对分层（百 / 千 / 万 / 十万）判断阶段最实用。
- 09 换赛道判据收敛为单一规则，删除正反两面都有的矛盾条款。

### 版本统一与 README 诚实化（D16/D17）
- SKILL.md frontmatter `version` → 1.2.0；README 版本号 → 1.2.0；本 CHANGELOG 新增 v1.2.0 条目，并修正 v1.0 失真条目（见下）。
- README 删除 Cursor / Windsurf 臆测安装路径（两家无公开的 skills 目录自动发现机制），只声明确定支持平台（Claude Code 及任何遵循 `SKILL.md` 约定的 agent），其余标注「未验证，自行适配」。
- README 删除「完全脱敏，不含任何个人姓名」绝对声明，改为「方法论脱敏；人设锚点为示例，用户需自建」。

---

## v1.1.2 — 移除 ear-style 引用 + 更换二维码（2026-07-16）

### 变更
- **移除 ear-style / 七把武器 全部引用**：ear-style 是作者个人口语表达体系，不适合公开 skill。04 的「语气速查（内置兜底）」升级为本 skill 自带的**「语气指南」**（唯一，不再是备选），写稿语气链路完全自包含、照常完整。涉及 SKILL.md / 04 / README / TROUBLESHOOTING / 示例对话 / CONTRIBUTING。
- **更换微信二维码**为更清晰的版本（`assets/wechat-qrcode.jpg`）。

---

## v1.1.1 — 作者支持入口（软 CTA）（2026-07-16）

### 新增
- **作者支持入口（软 CTA）**：新增 `SUPPORT.md`（付费 1v1 咨询菜单 + 微信二维码 `assets/wechat-qrcode.jpg`）；`SKILL.md` 增加「作者支持」使用纪律——只在用户主动求助、或命中三类高 stakes 卡点（定位拿不准 / 反复验证失败 / 瓶颈期）时提一次，其余情况绝不主动推销；`README.md` 加「作者支持」段。
- 本 skill 所有功能仍完全免费自助，咨询是可选支持，不是使用前提。

---

## v1.1 — 一致性 / 安装门槛 / 伦理护栏修复（2026-07-16）

面向开源首发的首轮系统性修补。北极星目标：任何人丢 GitHub 链接自装后，无需任何私有文件、无需其它 skill，就能直接当 IP 陪跑教练用。本次消除断链、硬依赖、幻影引用、自相矛盾与伦理裸跑。

### 方法论一致性

- **维度数统一为十（00-09）**。README 此前写"九"、部分内部文档写"八"——一律修正为十个维度文件，并在仓库所有提及维度数处对齐。
- **心法数统一为五条**。`00-心法与反模式` 与 SKILL.md 对齐：三条核心判断心法（判断力 / 博弈 / 用户思维）+ 两条执行心法（落实日常计划 / 开机比研究快），不再以"核心三条"充当总数。
- **显式调和两处张力**：①「开机比研究快（单条情绪保鲜）」与「先搭选题库（系统工程）」是两个层面——单条执行要快，选题系统要提前蓄水；②「判断力高于执行力」≠「只想不做」，判断力来自有控制变量的小步执行。
- **四相 ≠ 四阶段术语隔离**。四相（诊 / 契 / 行 / 盘）= 每次会话的主循环；四阶段（起号期 / 稳定上升期 / 瓶颈期 / 爆款后续航）= 长期生命周期。dossier 同时存"当前相"与"当前阶段"两个字段。
- **契相跳过条件收紧**。删掉"本次只做局部调整不涉及新发布→可跳"这个漏洞；唯一可跳情形：纯诊断且本周期明确不发任何内容、也确实没有任何值得记录的判断。
- **价值分层定义**调和 01 vs 02 冲突：价值 = 工具价值 + 情绪价值 + 身份价值三层，01 的"纯情绪不够"指"无工具落点难起号"，不否定情绪价值本身。

### 幻影引用清理

- 删除对**从未存在**的维度文件（钩子维度 / 标题封面维度 / 运营维度 / 拍摄呈现维度）的引用。
- 重映射：钩子 / 标题封面 / 开头 → `03-脚本骨架.md` 或 `02-选题方法论.md`；运营 → `07-复盘与执行.md`；拍摄呈现 → `05-表现力与镜头.md`。原则：要么指向 00-09 真实文件，要么就地内联，绝不再出现 00-09 之外的维度文件名。
- 修 `07-复盘与执行` frontmatter 里的断链 upstream：`03-脚本与结构` → `03-脚本骨架`；`04-钩子与开头` → `04-口播文案`。

### 安装门槛与零依赖

- **ear-style 改为可选增强**。删掉所有"必须加载 ear-style / 不加载=违规"表述。未安装时走 `04-口播文案.md` 末尾新增的「语气速查（内置兜底）」小节，写稿链路不断。SKILL.md 与 README 同步声明 optional。
- **README 重写为零基础友好**：可复制的 `git clone` 一行、主流 agent skills 目录路径表、装好如何验证（让 agent 复述四相）、首次 `cp templates/dossier-template.md ip-dossier.md`、ear-style 可选说明、无档案退化为单次咨询模式。
- **新建 TROUBLESHOOTING.md** 收常见坑：skill 没被识别 / ear-style 没装 / 首次没有 `ip-dossier.md` / `ip-contracts/` 何时建 / agent 读不到 references。
- **新建 docs/示例对话.md**：一段完整的首次到来 → 诊相建档 → 契相签约 → 行相出脚本 → 下次盘相复盘示例。

### 状态模型重构

- 契约不再塞进 dossier 的三张大表。改为每次签契约 → 新建独立文件 `ip-contracts/C-YYYYMMDD-NN.md`（由 `templates/contract-template.md` 复制）。
- dossier 只保留一个只读的**契约索引**小表（编号 / 选题 / 状态 / 下次复盘日）+ 认知沉淀区 + 当前相 / 阶段 / 锚点。
- 启动协议扫描 `ip-contracts/` 重建索引，提醒待复盘契约。SKILL.md、`08-陪跑闭环与档案`、dossier-template 三处一致描述。

### 启动协议与档案健壮性

- 在"读到档案 / 没有档案 / 拒绝建档"之外补**错误处理分支**：档案损坏或 schema 过期（询问备份后重建 / 手动修复）、多设备并发写（只读 + 提示冲突）、写回失败（原子写 + 保留备份）、拒绝读档（单次咨询模式不写回）。
- dossier-template 顶部加 `schema_version` 与升级说明，配合错误处理。
- 模式 A/B 切换规则从文件末尾**上提到显眼位置**；明确"单次咨询模式下，人设红线 = 用户当场声明的底线，未声明则主动问一句"，解决红线落空。

### 伦理护栏

- 所有"给希望 / 给情绪出口 / 给理由"类指导就近附带事实红线：希望感与情绪出口必须建立在真实基础上——不得编造数据、案例、资质、疗效、收益、合作关系；观点可以给力量，事实不可造假。
- `03-脚本骨架` 里关于"观点可给希望，但事实不可造假"的洞察保留，护栏前置强化。

### 平台适配

- 播放量分层（5 千 / 5 万 / 十几万 / 破百万）默认抖音口径，出现处分层就近加说明：小红书 / B 站 / 视频号 / 公众号等绝对口径不同，判断阶段改用"同赛道相对位次（如近 30 天该账号自身中位数）"而非绝对数字。

### references frontmatter 标准化

- `references/00-09` 全部加统一 frontmatter：`dimension` / `role` / `upstream` / `covers`。upstream 只填真实存在的文件名，断链全部修掉。

### 边界声明

- 行相弹药范围自洽：能力清单含 05 表现力，引用写准确的 01-06（含 05）。
- `05-表现力与镜头` 调色等后期操作加边界声明——本维度只给认知原则，不展开剪辑层操作。
- `04-口播文案` BGM / 音乐卡点 / 循环剪辑标注"属后期 / 剪辑层，本维度只点到为止"。

### 新增公开示例

- **新建 `references/定位锚点-示例.md`**：一份脱敏的通用定位锚点示例（虚构创作者），供开源用户照抄格式。不含任何真实个人信息。
- 耳总私人 `references/私有定位锚点文件.md` 保持私有（`.gitignore` 继续排除），所有"先读私有定位锚点文件.md"硬引用已删除，改为指向 dossier 锚点字段 + 示例文件。

### 文档基建

- **新建 CONTRIBUTING.md**：提 issue / PR 流程，强调方法论内核稳定、欢迎修文档 / 示例 / 平台适配。
- **新建 CHANGELOG.md**（本文件）。

---

## v1.0 — 初版开源（2026-07-09）

首次公开发布。把一线 IP 实战与大量真人案例拆解中反复打磨的方法论，经脱敏后浓缩为一套 IP 打造 skill。

### 实际包含（v1.2.0 修订，此前条目失真已修正）

- **SKILL.md**：skill 入口。早期版本的方法论内核与定位 / 选题 / 文案的判断心法，尚未引入四相工作流与陪跑闭环机制。
- **references/（实际 8 个维度方法论文件）**：v1.0 当时仅有 8 个维度文件——此后的「心法与反模式」「陪跑闭环与档案」「阶段与节奏」等维度与维度数统一到 10（00-09）是 v1.1 引入并补齐的，v1.0 时尚未到位。
- **README.md**：面向用户的安装与使用说明。
- **LICENSE**：MIT。

### 此前条目失真说明（v1.2.0 修订）

此前 v1.0 条目曾把以下内容列为 v1.0 已有，与事实不符，现予修正——这些都是**后续陪跑化重构（v1.1 起）才引入**的：

- **诊 / 契 / 行 / 盘四相工作流** —— v1.1 状态模型重构后引入。
- **`templates/dossier-template.md` 与 `templates/contract-template.md`** —— 陪跑档案模板与判断契约模板，v1.1 状态模型重构时新增。
- **陪跑闭环 / 跨会话档案 / `ip-contracts/` 契约独立存档** —— 同上，v1.1 引入。
- **references/00-09 十维度完整集** —— v1.1 才补齐到十个维度。

### 定位

装进 agent 上下文即用——覆盖定位 → 选题 → 脚本 → 文案 → 表现 → 变现 → 复盘全链路的判断心法；v1.0 版本不含跨会话陪跑机制（陪跑机制 v1.1 起引入）。明确不碰视频剪辑后期与视觉动效（交给 `jianying-editor` / `video-use` / `hyperframes`）。

### 已知局限（v1.0 → v1.1 修复目标）

- 维度数 / 心法数在个别文档与 README 不一致。
- `references/私有定位锚点文件.md` 私有，但部分正文写"先读"造成硬引用。
- ear-style 被表述为"必须加载"。
- 存在对未建文件（钩子 / 标题封面 / 运营 / 拍摄呈现维度）的幻影引用。
- 播放量分层默认抖音口径未声明平台差异。
- 缺 TROUBLESHOOTING / CONTRIBUTING / CHANGELOG / 示例对话等开源基建文档。

以上均已在 v1.1 修复。
